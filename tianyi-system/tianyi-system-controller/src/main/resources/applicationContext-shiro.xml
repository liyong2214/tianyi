<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!--包扫描-->
    <context:component-scan base-package="com.tianyi.system.session,com.tianyi.system.cache,
                                          com.tianyi.system.exception,com.tianyi.system.filter"/>

    <!--shiro过滤器-->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
        <property name="securityManager" ref="securityManager"/>
        <property name="loginUrl" value="/login.html"/>
        <property name="unauthorizedUrl" value="/403.html"/>
        <property name="filterChainDefinitions">
            <value>
                /login.html = anon
                /doLogin = anon
                /logout = logout
                /** = authc
            </value>
        </property>
        <property name="filters">
            <map>
                <entry key="rolesOr" value-ref="rolesOrFilter"/>
                <entry key="logout" value-ref="logoutFilter"/>
            </map>
        </property>
    </bean>

    <!--添加过滤器-->
    <bean class="com.tianyi.system.filter.RolesOrFilter" id="rolesOrFilter"/>
    <bean class="org.apache.shiro.web.filter.authc.LogoutFilter" id="logoutFilter">
        <property name="redirectUrl" value="/login"/>
    </bean>

    <!--创建SecurityManager对象-->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <property name="realm" ref="customerRealm"/>
        <property name="sessionManager" ref="sessionManager"/>
        <property name="cacheManager" ref="cacheManager"/>
        <property name="rememberMeManager" ref="rememberMeManager"/>
    </bean>

    <!--自定义realm-->
    <bean id="customerRealm" class="com.tianyi.system.shiro.realm.CustomerRealm">
        <property name="credentialsMatcher" ref="credentialsMatcher"/>
    </bean>

    <!--加密-->
    <bean id="credentialsMatcher" class="org.apache.shiro.authc.credential.HashedCredentialsMatcher">
        <property name="hashAlgorithmName" value="md5"/>
        <property name="hashIterations" value="1"/>
    </bean>

    <!--sessionManager-->
    <bean id="sessionManager" class="com.tianyi.system.session.CustomeSessionManager">
        <property name="sessionDAO" ref="redisSessionDao"/>
    </bean>
    <!--自定义session-->
    <bean class="com.tianyi.system.session.RedisSessionDao" id="redisSessionDao"/>
    <!--缓存管理-->
    <bean class="com.tianyi.system.cache.RedisCacheManager" id="cacheManager"/>
    <!--Cookie管理-->
    <bean class="org.apache.shiro.web.mgt.CookieRememberMeManager" id="rememberMeManager">
        <property name="cookie" ref="simpleCookie"/>
    </bean>

    <bean class="org.apache.shiro.web.servlet.SimpleCookie" id="simpleCookie">
        <constructor-arg value="rememberMe"/>
        <!--过期时间-->
        <property name="maxAge" value="2000"/>
    </bean>
</beans>